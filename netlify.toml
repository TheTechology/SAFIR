# Netlify deployment configuration for SAFIR
# Static site - no build step needed, publish directly from /static

[build]
  publish = "static"

  [build.environment]
    PYTHON_VERSION = "3.11"

    # Security headers for all pages
    [[headers]]
      for = "/*"
        [headers.values]
            X-Content-Type-Options    = "nosniff"
                X-Frame-Options           = "SAMEORIGIN"
                    Referrer-Policy           = "strict-origin-when-cross-origin"
                        Permissions-Policy        = "camera=(), microphone=(), geolocation=()"
